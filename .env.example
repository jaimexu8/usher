# Discord bot token (required)
# Obtain from https://discord.com/developers/applications
DISCORD_TOKEN=your_discord_bot_token_here

# Clash of Clans Developer API key (required)
# Obtain from https://developer.clashofclans.com
# Must be whitelisted with your EC2 instance's Elastic IP
COC_API_TOKEN=your_coc_api_token_here

# Bot command prefix (default: !)
COMMAND_PREFIX=!

# Logging level: DEBUG, INFO, WARNING, ERROR (default: INFO)
LOG_LEVEL=INFO

# Path inside the container where the SQLite database is stored
# Should point to a directory backed by a Docker volume
SQLITE_PATH=/app/data/bot.db

# How often (in seconds) the bot polls the CoC API (default: 120)
POLL_INTERVAL=120
